Class CSession
	function user_login()				vérifie si le user est valide	
	function user_add()				ajoute le nouvel user dans la table
	function send_validation()			envoi un email de validation
	function user_exist()				vérifie si l'user est dans la table
	function set_session($email, $Nom, $Prenom)	set les variables de session
	function user_list()				liste les users  (réservé superuser)
	function get_Profile()				recupère les info de sessions
	function kill_session()				stop la session et les variables de session
	static function doc()				return le contenu du fichier documentation
